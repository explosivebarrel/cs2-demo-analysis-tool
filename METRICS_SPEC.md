# METRICS_SPEC.md — семантика метрик: зачем, что показывает, когда срабатывает

Этот документ — **первичный** по отношению к коду: сначала семантика метрики
(какой игровой вопрос она отвечает), потом реализация. `METRICS.md` остаётся
краткой инженерной сводкой; здесь — эвристики, окна, случаи включения/исключения
и открытые вопросы.

Легенда вердикта:
- ✅ **ок** — семантика ясная, соответствует названию и назначению;
- ⚠️ **спорно** — метрика считается честно, но её интерпретация/порог сомнительны;
- ❗ **проблема** — название/смысл расходятся с тем, что реально считается.

Правило работы: любое семантическое изменение метрики начинается с правки этого
файла, затем код, затем `METRICS.md`.

---

## 0. Атом аналитики: дуэль

Почти все «умные» метрики построены на дуэли, поэтому фиксируем её определение
один раз.

**Дуэль** = килл-событие `player_death`, где атакующий и жертва — игроки
противоположных команд, произошедшее внутри распознанного раунда. Для игрока X
эпизод «won», если X — атакующий, «lost», если X — жертва.

- **Тик первого выстрела** — последний `weapon_fire` X не позднее тика килла в
  окне 3 с (`TTK_MAX_SEC`). Все «стартовые» оценки дуэли (скорость, walking,
  попадание первой пули) считаются на этом тике, потому что к моменту килла
  атакующий уже мог переехать/остановиться.
- **Попадание первой пули** — `player_hurt` врагу в течение 0.15 с
  (`BULLET_HIT_WINDOW_SEC`) после первого выстрела, только по врагам.
- **Группа дуэлей** — выстрелы X, сгруппированные с зазором > 2 с
  (`DUEL_MERGE_SEC`); каждый «старт» группы — кандидат в новую дуэль.
- **Контекст эпизода** — живые составы, ближайший тиммейт, flash_duration,
  скорости обеих сторон на тике первого выстрела (VelIndex, центральная
  разность по сэмплам).

Теги ошибок навешиваются на эпизод (раздел 4) и переиспользуются метриками.

---

## 1. Скоринг и раундовые агрегаты

### Rating (approx 2.1) — `player.rating`
- **Зачем**: одно число «насколько игрок хорош в этом матче», сравнимое между
  игроками.
- **Показывает**: взвешенную сумму саб-скоров: киллы 0.30, выживание 0.25,
  урон 0.20, KAST 0.15, импакт 0.10 (`RATING_WEIGHTS`).
- **Окно**: весь матч.
- **Случаи**: собственное приближение, НЕ официальный HLTV Rating 2.1.
- **Вердикт**: ⚠️ веса свои; для self-hosted ок, с HLTV сравнивать нельзя.

### RWS — `player.rws`
- **Зачем**: доля вклада в выигранные раунды.
- **Показывает**: среднюю долю урона в выигранных командой раундах
  (собственное приближение, `_rws` в run.py).
- **Окно**: весь матч, только выигранные раунды.
- **Вердикт**: ⚠️ приближение; семантика «вклад в победы» ясная.

### IMP (round impact) — `player.imp`, `series[].imp`
- **Зачем**: объяснительная оценка «что раунд принёс» + топ лучших/худших
  раундов.
- **Показывает**: очки за раунд: opening kill +2.0 в выигранном / +0.5, opening
  death −0.5 в выигранном / −1.0, каждый килл сверх первого +0.5, выигранный
  клатч +1.5, выживание в проигранном раунде +0.2.
- **Окно**: раунд.
- **Вердикт**: ⚠️ веса эвристические «на глаз». Спорно: +0.2 за выживание в
  проигранном раунде поощряет пассивность, хотя чаще это зажатое оружие
  (saves считаются отдельно).

### KAST — `player.kast`
- **Зачем**: доля раундов с любой полезностью.
- **Показывает**: % раундов, где был килл, ассист, флеш-ассист, выживание ИЛИ
  смерть отомщена (trade) в течение 5 с.
- **Триггер**: любой из компонентов на раунд.
- **Окно**: раунд; trade-компонента — 5 с (`TRADE_WINDOW_SECONDS`).
- **Вердикт**: ✅ классическое определение (flashAssist считается осознанно).

### MVP раунда — `rounds[].mvp`
- **Зачем**: главный contributor победы в раунде.
- **Показывает**: игрок победившей команды с max уроном за раунд.
- **Вердикт**: ⚠️ эвристика (в FACEIT-демках нет round_mvp): урон, а не киллы/
  клатч/дефьюз.

---

## 2. Базовые счётчики

### Kills / Deaths / Assists — `player.kills/deaths/assists`
- **Триггер**: `player_death` внутри распознанного раунда.
- **Случаи**: килл не засчитывается при суициде (`a == v`); тимкилл засчитывается
  как килл; смерти — все, включая суициды и мир; ножевой раунд/разминка — нигде.
- **Вердикт**: ⚠️ тимкиллы в kills — спорно (большинство стат-систем их не
  считает); на демках без TK не проявляется.

### K/D, KPR, DPR, APR — `player.kd/kpr/dpr/apr`
- **Зачем**: нормировка счётчиков на смерти/раунды.
- **Вердикт**: ✅ (APR включает flash-ассисты осознанно).

### ADR — `player.adr`
- **Зачем**: темп нанесения урона.
- **Показывает**: урон врагам / раунды.
- **Случаи**: урон по своим не считается; overdamage (dmg_health сверх остатка
  HP) входит — стандартно.
- **Вердикт**: ✅.

### HS% — `player.hsPct`
- **Показывает**: долю киллов в голову (по таблице оружия).
- **Вердикт**: ✅.

### UDR / dmgTaken — `player.udr/dmgTaken`
- **Показывает**: урон гранатами врагам / полученный урон, за раунд.
- **Вердикт**: ✅ (в UI сейчас не рисуются).

---

## 3. Открытия, трейды, мультикиллы, клатчи

### Opening kill / death, success — `player.opening`
- **Зачем**: качество первых дуэлей раунда — где раунд «делается».
- **Триггер**: первый килл раунда (после фриза).
- **Показывает**: kills/deaths и % побед в первых дуэлях.
- **Окно**: до первого килла раунда (временного порога нет — opening по
  определению).
- **Вердикт**: ✅.

### Trade kill / traded death — `player.trades`, tradeKillPct / tradedDeathPct
- **Зачем**: умирает ли игрок «напрасно» и мстит ли команда.
- **Триггер**: смерть X; авенжер — тиммейт X, убивший убийцу в течение 5 с.
- **Случаи**: один килл-мститель = один trade kill (даже за несколько смертей);
  каждая отомщённая смерть = traded death; вне раундов не считается.
- **Показывает**: tradeKillPct = trade kills / kills (выше = лучше: смерть не
  наказана); tradedDeathPct = traded deaths / deaths (выше = лучше: команда
  рядом).
- **Вердикт**: ✅ (после 4bfdfd6).

### Multikills — `player.multiKills`
- **Триггер**: ≥2 килла врагов в одном раунде.
- **Вердикт**: ✅.

### Clutches — `player.clutches`
- **Зачем**: игра в меньшинстве.
- **Триггер**: X остался один против ≥1 живых врагов, тиммейты мертвы (момент
  формирования 1vX).
- **Показывает**: played/won, byX (1v1..1v5); durSec от формирования до конца
  раунда.
- **Вердикт**: ✅ (смерть до конца раунда = проигранный клатч — стандартно).

---

## 4. Дуэльные теги ошибок

Теги навешиваются на эпизоды; потребители: mainProblem banner, lostDuels
breakdown, discipline ProbBars, списки эпизодов, isolatedPct/shiftPeekPct.

### moving_shot — ❗
- **Зачем**: выстрел без остановки — точность в движении ниже.
- **Триггер/порог**: скорость атакующего на тике первого выстрела > 50 u/s.
- **Вердикт**: ❗ имя говорит «ошибка контрстрейфа», по факту — «выстрел в
  движении» (движение может быть осознанным джигглом или ранаутом). После фикса
  VelIndex класс вырос в разы; название и порог стоит пересмотреть.

### missed_first — ⚠️
- **Зачем**: первый выстрел решает дуэль; промах = потерянная инициатива.
- **Триггер**: нет hurt врагу в 0.15 с после первого выстрела.
- **Вердикт**: ⚠️ ок; не отличает «выстрелил мимо» от «стрелял только через 2 с
  после пика».

### flashed (контекст эпизода) — ✅
- **Триггер**: flash_duration атакующего на тике первого выстрела > 1.5 с.

### isolated — ⚠️
- **Зачем**: дуэли без поддержки — следствие плохой позиции.
- **Триггер**: ближайший живой тиммейт дальше 800 юнитов; последний выживший не
  помечается.
- **Вердикт**: ⚠️ фиксированный радиус не учитывает геометрию карты, но как
  эвристика читаема.

### shift_peek — ✅
- **Триггер**: is_walking на тике первого выстрела (дисциплинный признак, не
  ошибка).

### passive_angle — ⚠️
- **Триггер**: атакующий стоял >1.5 с до первого выстрела.
- **Вердикт**: ⚠️ смешивает «держал угол» (норма) и «пассивно стоял». Самый
  спорный тег; конфликт с angleControl (см. раздел 11).

### overshoot / undershoot — ✅ (после 1f993f3)
- **Зачем**: качество подводки прицела.
- **Триггер**: знак угловой ошибки меняется (overshoot) или монотонно растёт
  (undershoot) в окне от первого выстрела до килла.
- **Счётчик**: overshootCount = число won-дуэлей с тегом (единый источник с
  карточкой и страницей).

### strong_duel — ✅
- **Триггер**: килл в голову без других ошибок.

### outnumbered (фронт, lostDuels breakdown) — ✅
- **Триггер**: живых союзников меньше, чем врагов, в контексте эпизода.

### winProb@duel — `duels[].winProb` — ✅
- **Показывает**: winprob стороны игрока на кадре строго ДО килла (килл не
  «подтверждает сам себя»).

---

## 5. Утилити

### Флеш-семейство — `player.flashes`
- **Триггер**: бросок флешки (группировка по entity, гэп 5 с); эффекты из
  `player_blind`.
- **Параметры**: effective = враг ослеплён ≥ 2.2 с; blindSec — сумма по врагам;
  friendly — ослеплены свои.
- **Вердикт**: ✅.

### flashEfficiency — `metrics.flashEfficiency`
- **Показывает**: effective / thrown × 100 (качество бросков).
- **Вердикт**: ⚠️ контент страницы («дуэли в ослепе») — другой срез; подписи
  уточнены, при желании переименовать раздел страницы.

### utilDmg, grenade counts — ✅
- Урон гранатами врагам; счётчики бросков по типам.

### reloadErrors — `metrics.reloadErrors` — ⚠️
- **Зачем**: перезарядка с полным магазином = потерянный TTK в ближайшей дуэли.
- **Триггер**: `weapon_reload` при >5 патронах, в живых раундах.
- **Вердикт**: ⚠️ порог 5 произвольный; не отличает перезарядку за укрытием в
  тишине (норма) от «в бою».

---

## 6. Aim-механики

### counterStrafeErrors — ✅ (переименовано в UI, поле API сохранено)
- **Зачем**: дисциплина стрельбы — сколько выстрелов сделано в движении.
- **Реально**: число ВСЕХ выстрелов при скорости > 50 u/s за матч.
- **Показывает**: UI-название «Выстрелы в движении» (en "Shots while moving").
  Дуэльные эпизоды с движущимся выстрелом — отдельный тег moving_shot на
  страницах.
- **Вердикт**: ✅ семантика и название совпадают (решено 2026-09-07, кластер A).

### idealStrafePct — ⚠️
- **Зачем**: доля «чистых» открывающих выстрелов в дуэлях, завершившихся
  киллом.
- **Триггер**: первый выстрел группы (гэп > 2 с) с киллом в окне 2 с.
- **Показывает**: % таких выстрелов с vel ≤ 50.
- **Вердикт**: ⚠️ популяция пересекается с moving_shot, но уже (только
  килл-дуэли, только открывающие); подписи уточнены.

### firstBulletAcc — ✅
- **Зачем**: попал ли первым же выстрелом.
- **Триггер**: открывающий выстрел; hit = hurt врага в 0.15 с (только враги).
- **Показывает**: % попаданий + per-shot список `firstBulletShots`.

### ttk_ms — ⚠️
- **Зачем**: как быстро дуэль доводится до килла.
- **Триггер**: килл; первый выстрел в окне 3 с.
- **Показывает**: среднее (килл − первый выстрел), только победные дуэли.
- **Вердикт**: ⚠️ длинные размены тянут среднее вверх; как тренд — ок.

### reactionTimeMs / successfulReactionTimeMs — ✅ (переименовано в UI)
- **Зачем**: насколько быстро игрок готов стрелять, когда враг начинает пик.
- **Триггер**: килл, у которого у жертвы детектируется «начало движения»
  (скорость > 30 u/s) в окне 2 с до первого выстрела.
- **Показывает**: UI-название «Время до первого выстрела на пике» (en "Time to
  first shot on peek"): среднее (первый выстрел − начало движения жертвы);
  successful — только с попаданием первой пули.
- **Случаи**: pre-aim (жертва стояла) исключён; cap 1500 мс.
- **Вердикт**: ✅ название честно включает подводку (решено 2026-09-07,
  кластер A).

### overshootCount — ✅ — см. раздел 4.

### crosshairPlacementPct — ✅ (пересчитано, кластер B)
- **Зачем**: был ли прицел готов к дуэли ДО того, как враг начал пик.
- **Триггер**: дуэль с детектируемым началом движения жертвы (та же популяция,
  что у reaction time); в момент начала пика жертвы измеряется угол между yaw
  атакующего и направлением на жертву.
- **Показывает**: % дуэлей, где угол ≤ 25° (`CROSSHAIR_PLACEMENT_DEG`).
  `crosshairGoodTicks` — тики киллов дуэлей с хорошей подводкой (эпизоды для
  страницы).
- **Окно**: момент начала пика жертвы (скорость > 30 u/s), до первого выстрела.
- **Вердикт**: ✅ настоящая семантика размещения прицела (решено 2026-09-07).

### excellentContacts — ✅ (после 1f993f3)
- **Зачем**: «идеальный контакт»: остановился → первая пуля попала → килл.
- **Параметры**: vel ≤ 50 на открывающем выстреле, hit в 0.15 с, килл в 2 с.
- **Эпизоды**: точные, `excellentContactTicks` (тики киллов).

### angleControlCount / ByPhase — ⚠️
- **Зачем**: сколько разных позиций «держал» ≥ 2 с.
- **Триггер**: серия сэмплов < 30 u/s длиной ≥ 2 с; дедуп сеткой 64 юнита.
- **ByPhase**: early ≤15 с / mid ≤45 с / late от начала раунда.
- **Вердикт**: ⚠️ «стоял» ≠ «держал угол»: стояние в открытом поле тоже
  считается. Как позиционная активность — ок, как «контроль» — нет.

---

## 7. Движение и выживание

### distanceKm / aliveSecPerRound / survivalPct — ✅
- Суммарный путь по сэмплам; среднее время жизни; % выживших раундов.

### saves — `player.movement.saves`
- **Триггер**: выжил в раунде, который команда проиграла.
- **Вердикт**: ✅ стандартно.

### holdsCount — ⚠️
- **Триггер**: стоянка ≥ 5 с вне фриза, дедуп радиусом 90 юнитов.
- **Вердикт**: ⚠️ в UI не рисуется; дублирует angleControlCount с другими
  параметрами — оставить что-то одно.

---

## 8. Бомба и экономика

### plants / defuses / defuseAttempts / kits — ✅
- Триггеры: `bomb_planted`, `bomb_defused`, `bomb_begindefuse` (+haskit).

### avgSpend / totalSpend, buyTeam — ⚠️
- **Триггер**: `cash_spent_this_round` на фризе.
- **Классификация**: full ≥ $3900, force ≥ $1900, иначе eco (средний спенд на
  игрока).
- **Вердикт**: ⚠️ пороги фиксированные, не зависят от серии счёта; читаемо.

---

## 9. Матчевые кривые и моменты

### Win probability — `replay.winprob` — ⚠️ (осознанная эвристика)
- **Зачем**: контекст «насколько раунд был выигран» в любой точке матча.
- **Модель**: живые/5 + HP + снаряжение; пост-плант — бомбовое время (40 с,
  дефьюз 5/10 с, ETA до бомбы); детерминированные исходы — плато 0.05/0.95.
- Методика: METRICS.md + memory.

### Swing-моменты — `moments[type=swing]` — ✅
- **Триггер**: сдвиг winprob ≥ 20 п.п. за окно 8 с, строго внутри раунда,
  до 2 на раунд.

### decisionsCost — ✅
- **Триггер**: смерть; падение winprob команды ≥ 8 п.п. за 3 с после смерти
  (look-ahead не выходит за раунд).

---

## 10. Реестр констант

| Константа | Значение | Где используется |
|---|---|---|
| SHOOT_THRESHOLD | 50 u/s | moving_shot, counterStrafeErrors, idealStrafePct, excellentContacts |
| DUEL_MERGE_SEC | 2 с | группировка выстрелов в дуэли |
| TTK_MAX_SEC | 3 с | первый выстрел дуэли (ttk, reaction, aim-ошибка) |
| BULLET_HIT_WINDOW_SEC | 0.15 с | «первая пуля попала» |
| TRADE_WINDOW_SECONDS | 5 с | trades, KAST-T |
| FLASH_EFFECTIVE_SECONDS | 2.2 с | effective flashes |
| flashed-тег | >1.5 с | дуэль в ослепе |
| RT_WINDOW_SEC / RT_MAX_MS / RT_MOTION_THRESH | 2 с / 1500 мс / 30 u/s | reaction time |
| ISOLATED-радиус | 800 юнитов | isolated |
| passive_angle | стоял 1.5 с | passive_angle |
| ANGLE_HOLD_SEC / MOVE / GRID | 2 с / 30 u/s / 64 юнита | angleControl |
| HOLD_MIN_SECONDS / RADIUS | 5 с / 90 юнитов | holds |
| OPENING_PHASE / MID_PHASE | 15 с / 45 с | фазы раунда |
| BUY_FULL_MIN / FORCE_MIN | $3900 / $1900 | классификация закупа |
| BOMB_TIMER / DEFUSE | 40 с / 5+10 с | winprob пост-плант |
| SWING_WINDOW_SEC | 8 с | свинг-моменты |
| decisionsCost | 3 с / 8 п.п. | цена смерти |
| IMP-веса | +2/−1/клатч +1.5 | impact раундов |

---

## 11. Открытые семантические вопросы (кандидаты в правки)

1. ✅ counterStrafeErrors — решено: UI «Выстрелы в движении», поле API сохранено.
2. ✅ crosshairPlacementPct — решено: реальный угол прицела на пике (кластер B).
3. ⚠️ Бенчмарк-тиры counterStrafeErrors/overshootCount/excellentContacts/
   idealStrafePct/crosshairPlacementPct откалиброваны на старых популяциях —
   нужна рекалибровка (кластер C, отложен пользователем).
4. ⚠️ passive_angle vs angleControl: стоянка для одного — пассивность, для
   другого — контроль. Нужна договорённость: стоянка в открытой позиции =
   passive_angle, стоянка у укрытия = angleControl?
5. ⚠️ Тимкиллы в kills: исключать (как большинство стат-систем) или оставить?
   (кластер Д отложен пользователем).
6. ✅ reactionTimeMs — решено: UI «Время до первого выстрела на пике».
7. ⚠️ holdsCount vs angleControlCount — дубли; оставить angleControl.
8. ⚠️ flashEfficiency: переименовать раздел страницы («Duels while flashed») или
   вписать карточку-дубль на страницу.
